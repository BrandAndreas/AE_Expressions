// For this solution the checkbox is on the same layer
// Otherwise it looks like this:
// const checkbox = thisComp.layer("LAYERNAME").effect("ADBE Checkbox Control")(1);

const checkbox = effect("ADBE Checkbox Control")(1);
checkbox == 0 ? 0 : value;
